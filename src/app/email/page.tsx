import OneboxLayout from "@/components/OneboxLayout";
import EmailList from "@/components/EmailList";
import {
  EmailContent,
  LeadDetails,
} from "@/components/EmailDetails";

const OneboxPage: React.FC = () => {
  return (
    <OneboxLayout>

    </OneboxLayout>
  );
};

export default OneboxPage;
